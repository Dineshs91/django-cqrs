version: '2'
services:
  postgres_db:
    image: postgres
  mongo_db:
    image: mongo
  web:
    build: .
    command: python blog/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - postgres_db
      - mongo_db
